#include "pch.h"

INT wmain(int argc, LPWSTR argv[]) {

    if (argc < 2) {
        std::wcout << L"Usage: " << argv[0] << L" <PID>" << std::endl;
        return 1;
    }

    // basic 
    // /user: 
}